---
import { getCollection } from 'astro:content';
import Layout from '../../layouts/Layout.astro';

export async function getStaticPaths() {
  const blogEntries = await getCollection('blog');
  return blogEntries.map(entry => ({
    params: { slug: entry.slug }, props: { entry },
  }));
}

const { entry } = Astro.props;
const { Content } = await entry.render();
---

<Layout title={entry.data.title}>
	<article class="post-article">
		<header class="post-header">
			<span class="category">Trend Report</span>
			<h1>{entry.data.title}</h1>
			<div class="post-meta">
				<span class="author">GTB Editor</span>
				<span class="separator">•</span>
				<time>{entry.id.split('_')[0]}</time>
			</div>
		</header>

		{entry.data.image && (
			<div class="featured-image">
				<img src={entry.data.image} alt={entry.data.title} />
			</div>
		)}

		<div class="prose">
			<Content />
		</div>
		
		<div class="share-section">
			<p>이 정보가 유용했다면 공유해보세요!</p>
		</div>
	</article>
</Layout>

<style>
	.post-article {
		max-width: 720px;
		margin: 3rem auto;
	}
	.post-header {
		text-align: center;
		margin-bottom: 2.5rem;
	}
	.category {
		color: #2563eb;
		font-weight: 700;
		font-size: 0.9rem;
		text-transform: uppercase;
		letter-spacing: 1px;
	}
	h1 {
		font-size: 2.5rem;
		margin: 1rem 0 1.5rem;
		line-height: 1.3;
		word-break: keep-all;
	}
	.post-meta {
		color: #6b7280;
		font-size: 0.95rem;
	}
	.separator { margin: 0 0.5rem; color: #d1d5db; }
	
	.featured-image {
		margin: 0 -2rem 3rem;
		border-radius: 1rem;
		overflow: hidden;
		box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.1);
	}
	.featured-image img { width: 100%; display: block; }

	/* 본문 스타일링 (Tistory Book Club 느낌) */
	.prose {
		font-size: 1.125rem;
		line-height: 1.8;
		color: #374151;
	}
	.prose :global(h2) {
		font-size: 1.75rem;
		margin-top: 3rem;
		margin-bottom: 1rem;
		color: #111827;
		font-weight: 800;
	}
	.prose :global(h3) {
		font-size: 1.4rem;
		margin-top: 2.5rem;
		margin-bottom: 0.75rem;
		color: #1f2937;
		font-weight: 700;
	}
	.prose :global(p) { margin-bottom: 1.5rem; }
	.prose :global(ul) { margin-bottom: 1.5rem; padding-left: 1.5rem; }
	.prose :global(li) { margin-bottom: 0.5rem; }
	.prose :global(strong) { color: #111827; font-weight: 700; background: linear-gradient(180deg, transparent 60%, #bfdbfe 60%); }
	.prose :global(blockquote) {
		border-left: 4px solid #2563eb;
		padding-left: 1.5rem;
		color: #4b5563;
		font-style: italic;
		background: #f9fafb;
		padding: 1rem 1.5rem;
		border-radius: 0 0.5rem 0.5rem 0;
		margin: 2rem 0;
	}
	.prose :global(a) {
		color: #2563eb;
		text-decoration: underline;
		text-underline-offset: 2px;
	}
	.prose :global(img) {
		border-radius: 0.75rem;
		margin: 2rem 0;
		max-width: 100%;
		height: auto;
	}
	
	.share-section {
		margin-top: 4rem;
		padding-top: 2rem;
		border-top: 1px solid #e5e7eb;
		text-align: center;
		color: #6b7280;
	}
</style>